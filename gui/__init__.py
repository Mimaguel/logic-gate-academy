"""
gui package
=================
The PySide6 front-end for Sirkitry. `main_window.ui` (a
standard Qt Designer file) defines every screen's layout; `main_window.py`
loads it at runtime and wires it up to the game logic in `game/`.

Edit `main_window.ui` in Qt Designer / Qt Creator to change how any
screen looks - as long as you keep the existing objectNames, no Python
code needs to change. See the project README for the full list of
objectNames each screen depends on.
"""
